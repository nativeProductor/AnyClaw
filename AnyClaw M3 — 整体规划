本文档是 M3 的整体规划，聚焦五个主线模块的目标定义和核心架构，不涉及具体 PRD。M3 的核心命题是：让 Agent 从「开发者驱动迭代」进化为「智能体能自我学习驱动迭代」。
M2 → M3 的跨越
M2 解决了「能用」的问题：开发者把 Agent 配置好，它能稳定服务用户，有记忆、有工具、有产物。
M3 要解决的是四个更深的问题：
Agent 怎么变得更懂业务？M2 的 Agent 只知道开发者手动配置的内容。M3 要让它能主动学习外部知识，把经验沉淀为结构化记忆，越用越懂这个领域。
Agent 怎么在工作中自我优化？M2 的 Agent 做完任务就结束了。M3 要让它定期反思，把高频行为固化为技能，识别无效内容主动清理，持续提升工作质量。
Agent 怎么安全地连接外部世界？M2 的 Agent 靠 MCP 和沙箱连接外部系统，凭证散落各处无统一管理。M3 要建立凭证安全基础设施，让 Agent 以规范的方式借用能力。
Agent 怎么嵌入客户自己的产品？M2 的 Agent 只能在飞书里使用。M3 要支持通过 Web SDK 将 Agent 内嵌到客户自己的产品中，覆盖更多企业场景。
M3 由五个主线模块构成：① 学习系统、② 自省系统、③ 记忆系统升级、④ 凭证集成（AnyVault）、⑤ Web SDK 渠道。

---
主线一：学习系统
目标
让 Agent 能从外部知识中主动学习，将文档、网页、数据库内容内化为结构化的经验卡片，沉淀为经验卡片。开Agent可以更快更省的解决高频问题。
两种触发方式
被动触发：开发者在管理后台直接上传文档、文件、粘贴网页 URL，Agent 自动提取内容生成经验卡片。适合初始化知识库和政策更新。
主动触发：开发者配置定时任务，指定「去哪里学什么」——例如每天爬取竞品官网价格页、每周同步内网数据库中的产品目录。M3 先支持定时触发，未来扩展支持 Webhook。
学习流水线

```plaintext
触发（被动上传 / 主动定时抓取）
  → 内容提取（文档解析 · 网页抓取 · 结构化）
  → 知识卡片生成（提炼关键知识点 · 标注实体关系 · 版本管理）
  → 开发者审核确认（YOLO 模式下跳过，直接写入）
  → 写入行业知识库（语义检索 + 知识图谱双层）



关键设计
- 语义检索 + 知识图谱双层架构：先用语义相似度找最相关的卡片，再沿知识图谱的关联关系找到相关实体的上下文，两路结果合并后给 Agent 使用
- 卡片关联联动：某张卡片更新时，与它有关联的其他卡片自动标记「建议复核」，防止知识出现不一致
- 检索效率追踪：自动追踪每张知识卡片被 Agent 实际引用的频率，长期未被引用的卡片标记为「待清理」，减少无效知识
- 版本管理：每张卡片保留完整的修改历史，政策变更时旧版本可以回溯

---
主线二：自省系统
目标
Agent 定期对自身的工作内容进行结构化反思，产出两类成果：经验卡片（写入学习系统，沉淀为行业知识）和技能提案（写入技能系统，固化为可复用技能）。同时识别无效内容，给开发者提供成本优化建议。
自省分两个层次
轻量自省（每次任务结束后自动触发）：不消耗额外的 AI 推理成本，自动记录任务结果、使用了哪些工具、完成了哪类职责、消耗了多少 Token，写入程序记忆备用。
深度自省（定时触发，默认每天一次）：基于积累的轻量自省数据，产出完整的自省报告，包含：
- 经验卡片：Agent 从实战中提炼的高价值知识点，由开发者审核后写入经验卡片  YOLO模式下无需审核
- 技能提案：对于 Agent 反复成功完成（成功次数 ≥ X 次、成功率 ≥ Y%）的工作模式，生成技能草稿，由开发者审核后正式上线  YOLO模式下无需审核
- 岗位对齐评估：评估 Agent 近期行为是否还在岗位职责范围内（见下）
岗位对齐评估
自省系统的一个重要产出，是定期评估 Agent 的行为是否偏离了它应该做的事。偏离分两个方向：
- 能力外扩：Agent 开始做不属于它职责范围的事（比如报销助手被引导去处理 IT 工单）
- 职责遗忘：Agent 某些核心职责长期没有被触发，实际上已经不会做了
系统计算一个 0～1 的对齐分，分级触发不同的响应：
对齐分
状态
系统响应
0.8 以上
✅ 健康
正常运行，无需干预
0.6 ~ 0.8
⚠️ 轻度偏移发出告警，自动生成偏移原因分析报告，供开发者参考

0.4 ~ 0.6
🚨 中度偏移
暂停新知识自动写入；暂停技能提案生成；要求开发者确认后恢复
0.4 以下
🔴 严重偏移
冻结所有自动写入；暂停深度自省；通知开发者人工介入
对齐分和成本数据构成 Agent 健康状态的双维度看板，在管理后台持续可见。
技能提案门控
技能提案在进入审核流程前，需要通过两道检查：

```plaintext
任务模式达到「成功 X 次以上且成功率 ≥ Y%」
  → 检查一：当前对齐分是否达标？
      否 → 拒绝提案，避免偏移行为被固化为技能
      是 → 检查二：该模式涉及的工具是否全部在岗位允许范围内？
              否 → 拒绝提案 + 告警
              是 → 生成技能草稿 → 开发模式：进入开发者审核流程 / YOLO 模式：直接上线


---
主线三：记忆系统升级
目标
M2 的记忆写入逻辑由系统自动执行，开发者无法干预。M3 开放记忆生成的干预能力，让开发者能在记忆写入流程中注入自定义提示词，控制记忆提取的侧重点、格式和边界。
记忆生成调优
开发者在干预记忆生成逻辑时，需要可以对干预效果进行调优。我们提供 一批高频内置的对话案例 让开发者可以快速预览到记忆的生成效果。也支持 开发者和智能体进行真实对话，并查看记忆的生成效果
干预示例
介入点
默认行为
开发者可以做什么
用户记忆提取
从对话中自动归纳用户偏好、历史、习惯
指定关注哪些维度（如「重点记录用户的预算上限和审批权限」）；排除哪些内容不写入
群组记忆提取
从群内对话中归纳群组整体上下文和关键决策
定义哪类对话值得沉淀（如「只记录包含决策结论的讨论，忽略日常闲聊」）
M3 可探讨的记忆升级方向
以下为 M3 可探讨的方向，具体是否做、做到什么程度，需进一步对齐。
定向跨场景记忆召回：M2 采用完全隔离策略——张三的单聊记忆、群聊记忆各自独立，互不影响。M3 可考虑有限度地打通：群聊中产生的记忆（本就是公开信息）可以在单聊中有限召回，但反向依然禁止（单聊中的私人内容不能在群聊中出现）。

---
主线四：凭证集成
目标
建立统一的凭证管理平台，解决 Agent 连接外部系统时的安全隐患。Agent 不再直接持有密钥和密码，通过 AnyVault 按需借用能力——用完即还，不留痕迹。
详细设计见：AnyVault — 智能体凭证与账号连接中心
核心解决的问题
- 凭证管理混乱：密钥和token等信息散落在不同的配置文件（如MCP或内置技能）里，难以管理
- 权限失控：Agent 持有长期有效的高权限凭证，一个只需要「读数据」的任务却拿着「读写删数据」的权限
- 用户授权链路断裂：仅支持飞书链路的用户授权，外部系统无法支持用户授权场景
AnyClaw 需要建设的部分
开发者侧（编辑态新增「集成」配置区）：
- 声明 Agent 需要哪些外部连接（连接清单），Agent 只知道连接名称，不接触实际凭证
- 配置开发者级凭证：系统凭证（内网数据库等）、应用令牌（第三方 API Key 等），所有用户共用
- 查看各连接的配置状态和用户授权情况
- 审计日志：谁用了哪个连接，什么时候，做了什么操作
终端用户侧（运行态飞书卡片授权）：
- Agent 执行任务时发现缺少用户授权，在飞书中向用户推送授权消息卡片
- 卡片展示：哪个 Agent 在申请、要连接哪个系统、用途说明、权限范围
- 用户在卡片上完成授权（输入账密 / OAuth2 跳转），无需离开飞书
- 授权完成后 Agent 自动继续执行被中断的任务
- 用户可在「连接中心」查看自己的所有授权记录，随时撤销
三类凭证在 AnyClaw 中的映射
凭证类型
典型场景
谁配置
在 AnyClaw 中的体现
系统凭证
内网数据库、内部 API
开发者
在「集成」配置中填写，类似 TAT 授权逻辑，所有用户共用
应用令牌
第三方 SaaS 平台的 API Key
开发者 / 用户自带
开发者配置默认值；也可由用户填写自己的个人 Key
用户授权 Token
GitHub、Gmail 等需代表用户本人操作的场景
终端用户 OAuth 授权
飞书消息卡片引导授权，Agent 授权后自动继续任务

---
主线五：Web SDK 渠道
目标
M2 的 Agent 只能在飞书中使用（单聊、群聊）。M3 新增 Web SDK 渠道，让开发者可以将 Agent 以对话组件的形式内嵌到自己的产品或系统中，覆盖飞书生态之外的企业场景。
典型场景
场景
描述
为什么需要 Web SDK
客服系统内嵌
企业将 Agent 嵌入自有客服平台，用户在客服窗口直接和 Agent 对话
客服系统是自研或第三方产品，无法在飞书里提供服务
工单系统助手
在工单提交页面嵌入 Agent，帮助用户填写工单、自动分类、推荐解决方案
工单系统有自己的用户界面，需要在原有流程中无缝集成
企业门户内嵌
在企业内网门户、OA 系统、ERP 等产品中嵌入特定职能的 Agent
这些系统有自己的用户体系和界面，不依赖飞书登录
与其他模块的依赖和能力要求
Web SDK 是一个新渠道，但它接入的是同一套 Agent 能力。以下是它与 M3 其他模块产生交叉的地方：
① 记忆系统：外部用户 ID 映射
飞书渠道的用户记忆隔离，依赖飞书用户 ID 作为隔离维度。Web SDK 场景下，用户来自客户自己的系统，有独立的用户 ID 体系，与飞书账号没有关联。
记忆系统需要支持接受外部用户 ID（由客户系统传入）作为隔离维度，确保 Web SDK 场景下同样能做到「每个用户有独立的记忆空间，互不可见」；或支持整个外部系统作为一个用户（系统级用户）。
② 凭证集成（AnyVault）：Web 原生授权流程
飞书/Web渠道下，当 Agent 执行任务需要用户授权时，通过推送飞书消息卡片引导用户完成 OAuth 授权。这套机制在 Web SDK 场景下不可用——用户在客户的页面里，没有飞书消息卡片。
AnyVault 的用户授权链路需要提供 Web 原生的授权交互方式：在嵌入页面内弹出授权窗口，或跳转到授权页再回调。授权完成后同样自动恢复 Agent 的任务执行。
③ 产物管理：SDK 内独立入口
是否支持产物管理？交由接入方控制，平台提供配置能力
M3 五大模块关系与优先级
模块关系
学习系统 ↔ 自省系统：双向流动。自省系统产出的经验卡片直接写入学习系统；学习系统的高质量知识库，反过来让自省系统的反思更有参照系。
凭证集成 → 学习系统：学习系统的主动触发（定时抓取外部数据源）需要访问受保护的 API 和内网系统，凭证集成是前提。
记忆系统升级：横切所有与记忆相关的模块，控制写入质量。它的价值在学习系统和自省系统都上线、开发者有真实使用经验后才能充分体现。
Web SDK：相对独立，与其他模块无强依赖，可并行推进。
推进建议
第一阶段优先启动：凭证集成 + 学习系统（被动触发）+ Web SDK
- 凭证集成是基础设施，越早建立越好；被动触发学习（开发者上传文档）实现相对简单，能最快给开发者带来价值
- Web SDK 与其他模块无依赖，可以并行推进，优先满足有内嵌需求的客户
第二阶段：自省系统 + 学习系统（主动触发）+ 记忆系统升级
- 自省系统需要 Agent 积累足够多的任务数据才能产出高质量反思，不宜太早上线
- 学习系统的主动触发与凭证集成天然对齐，在凭证体系稳定后一起上线
- 记忆系统升级的干预能力，需要开发者在使用了学习系统和自省系统一段时间后，才知道真正需要干预什么
