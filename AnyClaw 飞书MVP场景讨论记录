项目背景
AnyClaw 是一个基于飞书的企业级数字员工构建平台，开发者可在平台上创建、维护和培训数字员工（AI Agent）。
核心架构基于 OpenClaw 内核，主要差异包括：
- 用户识别：可识别飞书中不同的用户，群聊中不同用户 @ 机器人时，使用对应用户的 user token 执行飞书操作
- M2规划：用户记忆隔离（User Memory）和独立知识库（Industry Knowledge）

---
M1 当前能力边界
M1 版本使用 OpenClaw 内核，Memory 统一共享（无用户隔离），无独立知识库。核心差异能力是：以不同用户的 user token 执行飞书操作。
已支持的飞书操作：
能力
可以做什么
群组
创建群、拉人、发消息、管理群成员
消息
发送消息/卡片、@人、读取消息
多维表格
读写记录、筛选、更新字段
云文档
创建、读取、编辑文档内容
飞书任务
创建任务、指派、设置截止日、更新状态

---
MVP 场景推荐（从简单到复杂）
场景按对记忆的依赖程度分为三层。记忆依赖越深，冷启动越难，但用户粘性和替换成本也越高。

---
🟢 第一层：无需记忆，每次独立处理
每次任务完全基于当前输入，记忆不参与。适合 M1 最早期验证。

---
场景一：会议纪要 → 飞书任务自动分发 ⭐ 最推荐首发
流程：
1. 开完会，把会议录音/文字稿发给 Agent
2. Agent 解析出待办事项、责任人、截止时间
3. 以会议组织者的 user token，在飞书任务里创建任务、指派给责任人、发消息通知
核心能力： 消息 + 任务 | 无需 Cron | 无需记忆
为什么首发： 冷启动最简单，输入一份纪要立即出结果，效果立竿见影，用户愿意主动传播。

---
场景二：值班排班助手
流程：
1. 多维表格维护排班表
2. 群里 @ Agent 查"今天谁值班"，Agent 查表回答
3. Cron 每天早上自动提醒当天值班人
4. 值班交接时自动创建交接任务、更新排班记录
核心能力： 多维表格 + 消息 + 任务 + Cron | 无需记忆
适合人群： 运维、客服、销售团队，排班需求普遍且固定。

---
场景三：竞品/市场情报助手
流程：
1. Cron 定时触发（如每天早上）
2. Agent 用 browser use 抓取竞品官网更新、行业新闻、定价页变化
3. Shell 对比本次与上次抓取内容的差异（diff）
4. 有变化时发消息到飞书群，同时更新多维表格里的竞品追踪记录
核心能力： 消息 + 多维表格 + Cron + Browser Use + Shell
为什么合适： 纯人工每天刷竞品网站极耗时，browser use 正好能操作没有开放 API 的外部网站；shell 做 diff 让"有没有变化"一目了然，不需要人来比对。

---
场景四：Git Hunter — 开源项目更新追踪
流程：
1. Cron 定时触发（如每天或每周）
2. Shell 对指定 Git 仓库执行 git pull，提取新的 commit、release note、changelog
3. Agent 将更新内容整理摘要，写入云文档（按版本归档）
4. 发消息到飞书群，附云文档链接
核心能力： 消息 + 云文档 + Cron + Shell
典型用途： 追踪团队依赖的开源项目（如 OpenClaw）有无 breaking change 或重要新特性，不需要人手动去 GitHub 盯。

---
场景五：数据报表助手
流程：
1. 用户 @ Agent "帮我出本周销售数据报表"
2. Shell 连接公网可访问的 MySQL，执行查询语句
3. Agent 分析数据，生成报表写入多维表格，同时输出云文档报告
4. 发消息通知相关人员
核心能力： 消息 + 多维表格 + 云文档 + Shell
为什么合适： 不需要数据平台开放 API，shell 直连 MySQL 即可；每次查询独立执行，无需记忆；报表结果直接落在飞书里，不需要额外工具查看。

---
🟡 第二层：利用记忆，效果随时间提升
记忆作为辅助，没有记忆也能跑，但有了记忆之后质量明显更好。

---
场景六：招聘进度追踪助手
流程：
1. HR 在多维表格维护候选人信息
2. 面试官在群里 @ Agent 发面试反馈（如"张三技术面通过，建议进HR面"）
3. Agent 自动更新多维表格状态，创建下一轮任务，通知相关人
4. （可选）@ Agent "把Boss直聘上新投简历的候选人同步进来"，browser use 登录招聘平台抓取候选人信息，shell 格式化后写入多维表格
5. （可选）@ Agent "查一下这家公司背景"，browser use 查企查查/天眼查，结构化后写入候选人记录
记忆在哪里起作用：
- 记住每个面试官的评价倾向（"张三评分一贯偏高"）
- 记住候选人的历史沟通记录，避免重复问同样的问题
- 记住各岗位的招聘偏好，辅助筛选建议
核心能力： 多维表格 + 任务 + 消息 + Browser Use + Shell（可选扩展）

---
场景七：客户跟进 CRM 助手
流程：
1. 销售在群里 @ Agent 口头记录跟进情况
2. Agent 把记录写入多维表格（客户名、状态、下次跟进时间）
3. Cron 到期自动提醒销售跟进
记忆在哪里起作用：
- 记住每个客户的背景、关注点、历史沟通情况
- 记住每个销售的跟进习惯，主动提示可能遗漏的客户
- 自动识别长期未跟进的客户并预警
核心能力： 多维表格 + 消息 + Cron

---
🔴 第三层：记忆是核心，越用越不可替代
记忆是这类场景的本质价值所在。团队用得越久，Agent 对团队的认知越深，替换成本越高。

---
场景八：团队项目助手 + 知识沉淀
项目执行和知识积累天然是一体的——项目过程中产生决策、踩坑、解法，这些正是团队知识的来源。
记忆会沉淀什么：
- 每个成员的角色、擅长领域和工作习惯（"王五提任务从不写截止日期，需要追问"）
- 项目历史决策和背景（"上周放弃方案A，原因是性能问题"）
- 当前项目状态和冲刺目标
- 团队处理过的典型问题和解法
- 谁是某个领域的专家
- 反复踩过的坑和注意事项
能做什么：
- 群里 @ Agent "整理一下刚才的讨论"，Agent 结合项目背景把任务指派给对的人
- 新成员加入时，Agent 主动介绍项目背景、团队分工和常见注意事项
- 有人问"上次那个方案怎么决定的"，Agent 直接回答，无需翻记录
- 有人分享解决方案，Agent 主动问"要不要记下来"，定期整理为云文档 Wiki
- 每次会议结束自动更新项目状态到多维表格
核心能力： 消息 + 多维表格 + 任务 + 云文档 + Cron | 深度依赖记忆

---
场景九：跨群协作风险追踪助手
记忆会沉淀什么：
- 跨团队的依赖关系（"A项目等B项目提供接口，B项目承诺下周三"）
- 各方的承诺和 deadline
- 历史风险和延期模式
能做什么：
- 每天早上主动发"今日风险提醒"，列出到期承诺和可能影响进度的依赖项
- 有人问"B项目接口什么时候好"，Agent 直接回答，因为记得上次的承诺
- 发现某个依赖已经 delay，主动 @ 相关负责人确认影响
核心能力： 消息 + 多维表格 + Cron | 深度依赖记忆

---
场景优先级汇总
层级
场景
核心能力
Cron
Browser/Shell
记忆依赖
冷启动
用户粘性
🟢 无记忆
会议纪要→任务
消息 + 任务
否
—
无
⭐
低
🟢 无记忆
值班排班
多维表格 + 消息 + 任务
是
—
无
⭐⭐
低
🟢 无记忆
竞品情报助手
消息 + 多维表格
是
Browser + Shell
无
⭐⭐
低
🟢 无记忆
Git Hunter
消息 + 云文档
是
Shell
无
⭐
低
🟢 无记忆
数据报表助手
消息 + 多维表格 + 云文档
否
Shell
无
⭐⭐
低
🟡 辅助记忆
招聘进度追踪
多维表格 + 任务 + 消息
否
Browser + Shell（可选）
低
⭐⭐
中
🟡 辅助记忆
客户跟进CRM
多维表格 + 消息
是
—
中
⭐⭐
中
🔴 深度记忆
团队项目+知识沉淀
全部能力
是
—
高
⭐⭐⭐
高
🔴 深度记忆
跨群风险追踪
消息 + 多维表格
是
—
高
⭐⭐⭐
高

---
推进路径建议
先用第一层跑通验证，用第二层建立使用习惯，用第三层建立替换壁垒。 从"会议纪要→任务"切入获得第一批口碑，再引导用户尝试团队项目助手让记忆开始积累——一旦 Agent 记住了团队的项目背景、决策历史和踩坑经验，用户就很难离开了。
