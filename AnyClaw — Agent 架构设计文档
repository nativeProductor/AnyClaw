架构说明
一、AnyClaw是什么
AnyClaw 是基于飞书的企业数字员工构建平台。开发者在平台上创建 Agent，为它配置岗位、投喂行业知识、安装技能，然后将它部署给企业员工使用。
和通用 AI 助手不同，AnyClaw 的 Agent 扮演的是一个有明确岗位的专家角色——比如财务报销专员、IT 服务台、HR 政策顾问。它只做岗位内的事，对岗位内的问题越答越好，不会今天帮你报销、明天帮你写代码。
与 OpenClaw（个人助理框架）相比，AnyClaw 面向企业场景做了三项根本性改造：
多用户隔离企业里有几十上百人同时和同一个 Agent 对话。张三的报销记录、李四的审批进度，必须彼此隔离，不能互相泄露。OpenClaw 的记忆体系是单用户设计，在这个场景下会直接串库。
知识自进化OpenClaw 的 Agent 只能靠开发者手动更新 Skill 来进化。AnyClaw 让 Agent 能主动学习行业知识——上传一份报销政策 PDF，它能自己读懂、消化、形成结构化认知，日后回答相关问题时直接调用。
企业级治理Agent 长期运行会悄然漂移：被用户引导做了岗位外的事，或核心职责长期没被触发而"遗忘"。AnyClaw 内置持续运行的对齐监控，自动发现偏差、分级告警、触发干预，不依赖人工巡检。
扩展能力：多 Agent 编排当单个 Agent 的能力边界不够用时，AnyClaw 支持将多个专职 Agent 组合成协作网络——Agent Team，允许一个Agent通过工具调用其他子Agent，将复杂任务拆解后分发给专职子 Agent 并行处理，再汇总结果。这是一项可选的进阶能力，详见第八章。

---
二、Agent 架构总览
AnyClaw 的核心目标是培养单个 Agent（数字员工）。每个 Agent 内部由五个层次构成，各层职责独立、协同运转，驱动一个 Agent 不断进化成长。一次对话的流转路径如下：
用户消息进来 → 记忆层注入岗位定义、行业知识、用户偏好 → 执行层调用工具完成任务 → 任务结束写回记忆层 → 每天汇总自省进化 → 治理层持续监控，发现问题自动干预。
暂时无法在飞书文档外展示此内容

---
三、身份层：岗位人设
岗位人设是 Agent 的出厂设置，由开发者配置，定义三件事：做什么（岗位职责）、怎么做（行为素养）、不能做什么（安全边界）。
以一个财务报销专员为例：
维度
示例内容
岗位职责
解答差旅、餐饮、培训等报销政策；协助填写报销单；查询审批进度；识别超标并引导走超标审批流程
行为素养
涉及金额必须引用政策原文；不确定的问题引导联系财务部，不猜测；回答默认简短，3 句话以内
安全边界
不得泄露其他员工的报销记录；不得代替审批人做决策；不得访问报销系统以外的内部系统
人设是整个 Agent 的行为根基，配置发布后系统会对其做完整性保护，防止 Agent 在自进化过程中悄然修改自己的角色边界。人设的修改只能由开发者通过管理后台完成，每次修改都会生成新版本快照。

---
四、记忆层：让 Agent 记住该记的、忘掉该忘的
Agent 的记忆分五层，核心区别是持久化程度和隔离范围：
层级
名称
持久化
隔离性
存什么
Layer 1
岗位人设
✅ 永久，只读
全局唯一
职责、素养、安全边界
Layer 2
行业知识
✅ 永久
所有用户共享
报销政策、审批流程、经验卡片
Layer 3
用户记忆
✅ 永久
严格用户隔离
每个用户的偏好、历史、沟通风格
Layer 4
会话上下文
⏳ 压缩重置管理
单用户
当前对话的完整历史，通过压缩/重置两种机制控制规模
Layer 5
程序性记忆
⏳ 临时，用完即清
不参与召回
自省提取的任务步骤，待沉淀为 Skill
4.1 行业知识：不是 RAG，是"让 Agent 真正学会"
传统 RAG 是"搜到哪段读哪段"——把文档切成碎片，用户问问题时拼凑几段扔给模型，知识是零散的，没有结构，也不会更新。
AnyClaw 的做法不一样。开发者上传政策文档后，Agent 会主动消化、理解，把知识提炼成一张张经验卡片——每张卡片对应一个独立的知识点，有标题、有来源、有适用场景，卡片之间还会建立关联关系，构成一张知识网络。
文档与卡片的关系
一份 PDF 文档通常会被拆解为多张卡片，每张卡片对应文档中一个独立的知识点，并保留原始出处。原始文档作为素材留存，卡片是 Agent 真正使用的知识单元——Agent 不会直接读 PDF，只读卡片。
举个例子，处理完一批差旅报销问题后，知识库里可能会形成这样几张卡片：
卡片标题
核心内容
关联卡片
一线城市住宿报销标准
北京/上海/广州/深圳，上限 600 元/晚（2026年2月起）
→ 超标审批流程→ 职级差异说明
超标审批流程
超标金额需附说明，由直属上级审批，超过 500 元需财务总监复批
→ 一线城市住宿报销标准→ 审批时效说明
职级差异说明
P7 及以上可申请豪华型，P6 及以下执行标准限额
→ 一线城市住宿报销标准
召回策略：三路并行，按需扩展
用户提问时，系统通过三种方式找到相关卡片，结果合并后注入给 Agent：
召回方式
原理
适合场景
语义召回
将问题转为向量，找语义相似的卡片
用户问法和卡片措辞不一样，但意思相近。例："住酒店能报多少"→ 命中"住宿报销标准"
关键词召回
基于关键词精确匹配
问题含明确术语或数字时补充语义召回的遗漏。例："P7 住宿"→ 精确命中"职级差异说明"
关联扩展
命中一张卡片后，自动拉取其关联卡片
避免只返回局部知识。例：命中"住宿标准"后，自动带入"超标审批流程"和"职级差异"
三路并行之后，系统对候选卡片按相关度打分排序，取 Top-K 张注入给 Agent。系统会追踪每张卡片是否真正被 Agent 引用——长期被召回但从未被引用的卡片会被标记为"僵尸卡片"，在自省报告中推荐清理，避免浪费 Token。
知识更新的联动机制：当住宿标准从 500 改为 600，系统不只更新那一张卡片，还会自动把关联的"超标审批流程"标记为"需要复核"，因为超标的判断基准已经变了。每张卡片保留完整的版本历史，也支持追溯"2 月之前的标准是什么"。
4.2 用户记忆：多人使用，互不干扰
企业里有几十上百人同时和 Agent 对话，Layer 3 通过三道防线确保用户间的记忆完全隔离：物理分区（不同用户的数据存在独立空间）、查询隔离（每次查询强制带上用户 ID 过滤）、输出审查（输出前扫描是否含他人信息）。
用户张三的记忆示例：习惯拍照上传票据；上次报销工单 RE-2026-0892 审批中；沟通偏好是简短回答。这些信息只有张三触发查询时才会被读取，对其他用户完全不可见。
4.3 会话上下文：压缩重置的两级管理
飞书 bot 场景下，用户和 Agent 只有一个长期对话窗口，上下文会随对话持续累积。AnyClaw 用两种方式控制 Ctx 规模：自动压缩：Ctx 占用达到 80% 时，自动对最早的若干轮对话做摘要压缩，摘要替换原文留在 Ctx，释放空间。可以无限次触发，用户无感知。主动重置（/new）：当上下文已多次压缩、可能存在记忆失真时，建议用户执行 /new 开启新会话。/new 直接清空 Ctx，不做任何归档——被清空的内容就是删除。
设计原则：Ctx 是推理用的工作区，不是记忆的载体。 用户偏好、已确认结论等有价值的信息，由 Agent 在日常交互中主动提炼写入 Layer 3，因此压缩后的内容会直接清空，无需重复写入Layer 3
4.4 程序性记忆：自省系统的"需求暂存池"
Layer 5 是五层记忆里最特殊的一层——它不是用来给 Agent 查询使用的，而是自省系统的中间产物。
每次任务完成后，自省系统会自动从会话记录里提取本次的执行步骤，写入 Layer 5。可以把它理解成一个待处理的需求池：攒够了足够多的同类步骤，说明这个任务模式足够高频，值得封装成 Skill。Skill 一旦审批上线，对应的步骤记录就会从 Layer 5 清除——它完成了使命，变成了更稳定的能力形态。
4.5 五层联动：一次对话的完整过程
张三：我上个月去北京出差，住宿花了 650 元，能报销吗？
Agent 内部如何处理：
Layer 1 确认这是合法的"报销查询"请求 → Layer 2 检索到"一线城市住宿标准 600 元"和"超标审批流程" → Layer 3 读到张三惯用拍照上传 → Layer 4 携带本次会话上下文
Agent 回复：北京属于一线城市，住宿标准是 600 元/晚，您的 650 元超标 50 元，需要走超标审批流程。您需要拍照上传票据吗？
任务结束后：本次步骤序列写入 Layer 5
五、执行层：Agent 的手和脚
执行层由四个模块构成。其中 Tool 是底层能力，Skill 是在 Tool 之上封装的业务单元；Cron 和人机协同是两种任务触发与控制机制，三者共同依赖 Tool 完成实际操作。
暂时无法在飞书文档外展示此内容
5.1 Tool：底层工具能力
Tool 是 Agent 与外部系统交互的底层通道，Skill 的实际执行依赖这五类工具：
工具类型
典型用途
说明
Sandbox Shell
调 API、处理数据、执行脚本
隔离的代码执行环境，支持通过代理通道访问企业内网
Browser Use
填表单、抓页面数据
自动化浏览器操作，支持登录态下的页面交互
MCP 协议
接入飞书、Jira、Salesforce 等
标准化外部服务集成协议，开箱即用
内网代理通道
访问 ERP、OA、HR 等内网系统
安全隧道 + 白名单管控 + 全量流量审计，企业数据不出内网
Sub-Agent 调用
调用专职子 Agent 处理子任务
主 Agent 像调用普通工具一样调用子 Agent，子 Agent 有独立的人设、知识库和 Skill，执行完毕将结果返回给主 Agent，详见第八章
5.2 Skill：封装高频场景
Skill 是"知识 + 执行"的打包单元——不只是告诉 Agent"这件事怎么做"的说明书，还包含了调用 Tool 实际完成这件事的代码逻辑。可以把 Skill 理解为 Agent 的"岗位技能包"，一个报销专员 Agent 可能有「查询审批进度」「超标报销引导」「政策解答」等多个 Skill。
Skill 有两个来源：开发者手动创建，或 Agent 在自省中发现高频模式后自动起草、由开发者审批上线。每个 Skill 有完整的生命周期管理，Agent 自省生成的 Skill 必须经过开发者审批，不能直接上线。
暂时无法在飞书文档外展示此内容
此外，平台会内置一个查询聊天记录 Skill：所有 session 的对话内容独立持久化存储，与记忆层和 Ctx 无关。Agent 通过这个 Skill 调用平台 API 按需查询历史对话，用于处理"X月X号我们聊了什么"、"帮我找一下之前关于XX的讨论"等需求。这是一个独立的数据查询能力，目标是让Agent在对话中更加拟人。
5.3 Cron：定时主动任务
Agent 不只能被动响应对话，还可以创建定时任务主动执行，从"等人问"变成"主动做"：
典型场景
触发时间
执行内容
周报自动生成
每周一 09:00
汇总上周报销数据，生成飞书文档并发送给财务负责人
超时工单催办
每天 10:00
扫描审批超过 5 个工作日的工单，自动发飞书消息催办
政策变更同步
每月 1 日
检测报销标准文档是否更新，有变化则触发知识库增量学习
Agent 健康报告
每月最后一个工作日
生成对齐状态 + 成本趋势月报，推送给开发者
5.4 人机协同：遇到关键节点，交给人来决定
复杂业务不能全自动。Agent 执行过程中遇到需要人工判断的节点时，可以主动挂起任务，通过飞书消息卡片通知指定人员，等对方做出决策后自动恢复，任务上下文完整保留，不会丢失进度。
以"内网数据库故障自动修复"为例——Agent 全程负责发现问题、定位根因、生成修复方案，但在执行变更前强制等待负责人确认：
暂时无法在飞书文档外展示此内容

---
六、进化层：越用越聪明
在AnyClaw的架构中，Agent 可以像真正的员工一样从工作中持续学习，而不是等开发者来更新它。整个进化机制围绕一个自省循环展开：
暂时无法在飞书文档外展示此内容
自省分两个层次：每次任务结束时做轻量记录，不调用 AI，几乎没有额外成本；每天凌晨 Heartbeat 守护进程做一次深度分析，产出自省报告。
自省报告覆盖四件事：
- 沉淀知识：发现反复出现的知识点，整理成新经验卡片写入知识库
- 提案 Skill：发现高频任务模式，起草 Skill 草稿推送给开发者审批；Skill 上线后，Layer 5 中对应的步骤记录自动清除
- 识别无效内容：找出长期被检索但从未用到的知识卡片，附上预估的月度浪费金额，推荐开发者清理
- 健康状态更新：计算岗位对齐分，发现漂移时触发分级告警
自省报告片段示例（报销专员 Agent，2026-02-25）
分析窗口：近 50 次对话 · 任务成功率 94% · 平均 2.3 轮完成
发现高频模式：本周 23 次"查报销标准"，每次都走知识库检索，耗时长→ 建议封装为独立 Skill，预计响应速度提升 60%，已生成草稿待审批
识别无效内容：卡片「出差补贴标准」检索 21 次、引用 0 次→ 建议移除
健康状态：岗位对齐正常 ✅，「异常上报」职责本周期未触发，建议关注
所有自省产出的变更——新 Skill、知识更新、内容清理——都需要开发者确认，Agent 不能自己直接上线任何内容。
七、治理层：让 Agent 保持在正轨上
7.1 岗位对齐监控
Agent 长期运行会悄然漂移，有三种典型路径：被培训了岗位外的知识、被用户反复引导形成了越界习惯、某项核心职责因长期没人问而实际上"遗忘"。前两种是行为外扩，后一种是行为内收，都需要被发现和纠正。
系统通过三个维度持续评估健康状态：
维度
✅ 健康
⚠️ 异常
岗位胜任度核心职责的完成质量
政策解答准确率 96%，用户无需二次追问
超标审批引导频繁出错，用户投诉增加
工作内容匹配度实际任务是否在岗位范围内
近 50 次任务中 92% 属于报销相关职责
18% 的任务涉及 IT 报修和 HR 查询，明显越界
职责覆盖完整性是否有长期休眠的能力
5 项核心职责本周期全部触发
「异常上报」30 天未触发，事实上已失效
系统每天自动计算综合对齐分，按四个级别响应，自动干预程度逐步升级：
暂时无法在飞书文档外展示此内容
一旦开发者介入修复（清除越界记忆、下线越界 Skill），下一次 Heartbeat 重算对齐分，恢复健康后自动解除所有限制，无需手动操作。
7.2 成本观测
Token 消耗是 AnyClaw 的核心运营成本。系统对每次任务的 Token 消耗按层拆解，让开发者清楚地看到钱花在了哪里。
行业知识层（Layer 2）通常占总成本的 50% 以上，是优化的主战场——知识库里堆了太多用不到的卡片、检索每次取回数量过多、卡片内容冗长但模型只看了前几句，这些都可以被精准识别并优化。
成本异常上升往往是行为漂移的早期信号。当 Token 消耗增加但请求量没变时，通常意味着 Agent 在处理越来越多岗位外的复杂任务——这时应该先检查对齐状态，再决定是否优化成本。
7.3 版本审计
Agent 的每一次变更都会生成版本快照，无论是开发者主动修改，还是自省产出的进化。以一个"项目协作助手"Agent 为例——它服务于多个团队，需要协调跨部门任务、同步项目进度、自动整理会议纪要：
暂时无法在飞书文档外展示此内容
每个版本记录：变更时间、变更类型（人工 / 自进化）、具体改动内容、触发人。支持版本 diff 对比和一键回滚，出问题时能快速定位是哪次变更引入的。

---
八、多 Agent 编排（可选扩展）
多 Agent 编排是 AnyClaw 的进阶能力，不是默认工作方式。绝大多数场景下，一个独立的 Agent 就能胜任许多复杂任务。当任务跨越多个专业领域、单个 Agent 无法胜任时，才需要引入编排。
8.1 两种 Agent 角色
AnyClaw 的 Agent 有两种存在形态，开发者在创建时选择：
类型
是否有对话渠道
典型用途
示例
有渠道 Agent
✅ 可在飞书、Web 等渠道直接对话
面向终端用户的数字员工，独立承接用户请求
财务报销专员、IT 服务台、项目管理员
无渠道 Agent
❌ 不对用户开放，只能被其他 Agent 调用
专职能力单元，作为子 Agent 参与编排协作
PRD 评审员、代码分析员
无渠道 Agent 有独立的人设、知识库、Skill、自省和版本审计——它和有渠道 Agent 的唯一区别是入口：前者只能被其他 Agent 调用，不能被用户直接找到。这让每个子 Agent 的能力边界、进化路径和治理状态都完全独立，不会因为参与编排而相互污染。
8.2 编排模式
编排不需要额外创建"主 Agent"——任何有渠道的 Agent 都可以成为编排的发起者。开发者只需要为它安装"调用子 Agent"这个 Skill，系统自动赋予该 Agent 编排能力：接收到超出自身能力边界的复杂任务时，它可以将子任务分发给专职子 Agent 处理，汇总结果后统一回复用户。对用户来说，始终只有一个对话入口，感知不到背后的多 Agent 协作。
暂时无法在飞书文档外展示此内容
8.3 完整案例：PRD 评审与代码影响分析
以"技术评审助手"为例——这是一个有渠道的普通 Agent，安装了 PRD 评审和代码分析两个子 Agent 的调用 Skill 后，就具备了跨领域的协作能力：
暂时无法在飞书文档外展示此内容
8.4 编排的治理边界
每个 Agent 无论是否参与编排，治理规则保持一致：
治理维度
多 Agent 场景的问题
处理方式
成本归因
多个 Agent 协作，Token 消耗分散
每个子 Agent 的 Token 消耗独立记录，可以基于调用记录追溯到主 Agent 的每个任务下，可按 Agent 维度查看成本明细
权限隔离
主 Agent 调用子 Agent 时可能越权
子 Agent 只能访问自身岗位白名单内的工具和数据，主 Agent 无法越权授权
对齐监控
子 Agent 被反复要求做岗位外的事而漂移
每个 Agent 的 JAS 独立计算，主 Agent 的调用行为不能绕过子 Agent 的岗位边界

---
九、参考调研文档
AnyClaw — 记忆系统设计
AnyClaw — 技能系统设计
AnyClaw — 岗位对齐度评估体系
AnyClaw — 成本观测与优化体系

---
十、Changelog
版本
日期
变更内容
v1.0
2026-02-25
初始版本，建立 AnyClaw 基础架构：身份层、记忆层（四层）、执行层、进化层、治理层
v1.1
2026-02-27
记忆层升级为五层（新增 Layer 5 程序性记忆）；Layer 1 增加 SHA-256 完整性保护；Layer 4 升级为 5 阶段渐进式压缩引擎
v1.2
2026-02-27
补充 Layer 2 行业知识召回策略（三路并行：语义召回 + 关键词召回 + 关联扩展）；新增僵尸卡片检测机制；完善 4.1 文档与卡片关系说明
v1.3
2026-02-27
新增治理层版本审计章节，替换为项目协作助手多团队扩展案例；Layer 5 新增生命周期管理（stale / deprecated / promoted）
v1.4
2026-02-27
新增成本观测体系：per-layer Token 归因、自省报告成本节、Layer 2 检索效率指标
v1.5
2026-02-28
新增第八章多 Agent 编排（可选扩展）：有渠道/无渠道 Agent 两种角色、编排模式、PRD 评审案例、治理边界；章节重排，编排章节调至最后
v1.6
2026-02-28
架构总览图重绘：主链路上下垂直布局，子 Agent 右侧虚线引出并有独立自省闭环；去掉 Orchestrator 概念，编排能力改为安装 Skill 后自动赋予
v1.7
2026-03-02
Layer 4 上下文管理机制重写：去掉"会话结束清空"，改为压力驱动的两级管理（滚动压缩 → 摘要沉淀），压缩内容占 Ctx 80% 时触发沉淀，保留最近 20% 原始对话；Layer 3 新增对话时间线与结构化记忆两类写入；新增 Milestone 章节（M1/M2/M3 + 并行建设），含版本管理和成本看板分阶段规划
v1.8
2026-03-03
Layer 4 上下文管理机制更新：不再沉淀压缩的上下文，改为压缩重置的管理模式。上下文可以无限压缩，如果用户感觉内容失真严重，可以手动重置，重置后压缩的上下文丢失，不再归档；新增查询Session聊天记录的技能
Milestone
[M1] 支持开发者快速部署openclaw内核的新版自定义智能体，打通飞书渠道并做好飞书集成体验   0313上线
1. 支持开发者在Aily管理后台创建管理openclaw版自定义智能体，无需手动配置飞书应用
2. 在飞书IM中与智能体对话时，支持展示COT并流式输出结果
3. 在飞书IM中与智能体对话时，支持收发文件/图片
4. 支持UAT/TAT授权与飞书进行集成，做好与消息、云文档、多维表格等常用功能的集成
[M2] 支持上下文压缩，支持多层记忆并做好用户记忆隔离，让Agent拥有和真实员工一样的长短期记忆
1. 做好session隔离，确保router的稳定性，杜绝"回复错seesion"、"回复乱序"和"重复回复"等问题   技术驱动
2. 支持session内的上下文压缩，确保Agent可以与员工长期同频对话
3. 做好对不同user/group的记忆隔离
4. 支持为Agent关联知识空间、关联本地文档
[M3] 建设Agent的培训学习机制，让Agent可以在工作中进化成长
1. 支持开发者可视化管理扩展技能库（引入外部技能时对接安全扫描）
2. 支持开发者开发技能，支持将工作流应用封装为技能
3. 建设Agent的自省机制，允许Agent定期异步获取任务/会话信息，完善自身的技能和知识空间
4. 建设Agent的岗位胜任度评估系统，从 能力胜任度、工作内容匹配度等多维度评估Agent的表现
[并行建设] 扩展自定义智能体的能力边界，Cover更多真实复杂的企业工作场景
1. 建设Agent的运营数据、成本数据看板，帮助开发者/管理员管理Agent   M2需要先支持轻量版
2. 建设Agent的版本管理机制，支持开发者审查每次版本变更内容并快速回滚  M2需要先支持轻量版
3. 支持Agent为客户部署代理通道，在沙箱中访问客户的内网环境
4. 支持人机协同，允许自定义智能体在执行任务的过程中挂起任务，等待审批后（邮件、卡片）继续执行
5. 建设Agent Team能力，支持Agent通过工具调用其他Agent完成复杂任务
