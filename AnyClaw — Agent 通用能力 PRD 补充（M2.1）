本文档是 M2 PRD 的补充，聚焦本次新增能力。原有 M2 能力维持不变，以原文档为准。
名词补充
名词
解释
开发者
由 Agent Owner 指定，拥有迭代 Agent 的权限。可以是飞书用户，也可以是飞书群组（群内所有成员均获得开发者权限，群成员变动时权限随群扩散）。
YOLO模式
开发者无需进入管理后台，直接在飞书群的对话中驱动 Agent 迭代的工作模式。每次迭代确认后立即生效，同时自动写入版本记录。
草稿态
开发者在管理后台保存但尚未发布的配置版本。/debug 模式下 Agent 使用草稿态运行。
Story 1：开发者管理
Owner 可以在管理后台指定开发者，开发者拥有与 Owner 相同的迭代权限，但不能转让 Owner 身份。
业务规则：
- 开发者可以设置为飞书用户或飞书群组
  - 设置为群组时，开发者权限随群扩散
- 开发者在两种模式下的权限：
  - 开发模式：可进入管理后台配置并发布智能体
  - YOLO 模式：可在飞书群对话中直接驱动智能体迭代
Story 2：YOLO 模式——在群里直接更新智能体
开发者无需进管理后台，在飞书群对话中直接驱动智能体修改自身配置。
业务规则：
- YOLO 模式下，开发者 在任何渠道中都可以通过对话让智能体完成自我更新操作，包括：修改人设、学习技能和引入 MCP 。修改无需二次确认无需发布
- 非开发者在渠道端的对话依然不会更新智能体，只有开发者的指令才会
- 每次发版后，同样需要写入版本记录（记录变更内容、操作人、时间），版本来源标注为 YOLO
- 开发者可以在管理后台 手动切换： 开发模式、YOLO模式 。切换时需二次确认
这两种模式只涉及 运行态是否可自我更新的差异，管理后台配置无差异
Story 3：在飞书群中快速创建智能体
开发者可以不进管理后台，直接在一个飞书群里创建并使用智能体。
业务规则：
- 开发者在飞书群中描述想要的智能体，系统根据描述自动生成全套配置（人设、工具、技能），创建后直接在该群上线，无需手动发布
- 创建后的智能体默认为 YOLO 模式
- 系统自动将创建时所在的飞书群加入该智能体的开发者列表，群内所有成员均获得开发者权限
- 群组内快速创建的智能体，同样可以在管理后台查看和修改智能体的完整配置
与管理后台创建的区别：
维度
飞书群内快速创建
管理后台创建（原 M2）
入口
飞书群聊
Aily 管理后台
配置方式
自然语言描述，系统给一套默认值
YOLO模式创建的智能体默认开启全部内置工具，默认不加载任何MCP和技能
逐项配置
默认模式
YOLO 模式
开发模式
初始开发者
Owner+创建时所在群，同样可在管理后台手动添加
只有Owner，可手动添加
- 在YOLO模式下，开发者将智能体邀请到一个新群聊时，智能体会自动发送一个"仅开发者可见"的消息卡片，让开发者确认 是否将该群聊添加为智能体的开发者
  - 如果是在飞书应用可用范围但不是智能体开发者的用户执行此操作，不会触发授权确认
Story 4：新增指令
智能体可以在渠道中接收下述指令：
/new（扩展至群聊）
- 适用范围：单聊、群聊均可用
- 行为：清空当前 session 的对话上下文，开启新 session。记忆不受影响
- 群聊注意：在群聊中发送 /new 会清空整个群的当前 session 上下文，影响群内所有成员
/stop
- 适用范围：单聊、群聊均可用
- 行为：立即终止智能体当前轮次的思考和输出
- 会话影响：不清空 session 上下文，下一条消息可在当前上下文基础上继续
/debug
- 适用范围：单聊、群聊均可用；仅开发者可触发，普通用户发送无效
- 前提：智能体在管理后台存在未发布的草稿态
- 行为：进入 debug 模式，智能体切换为使用当前最新草稿态运行
- 关键规则：
  - 进入 debug 模式后不清空当前 session 上下文，可在已有对话基础上继续测试
  - debug 模式期间，管理后台对草稿的修改实时生效（下一轮对话即使用最新草稿）
  - 同一群聊中，普通用户仍使用线上发布版本，开发者使用草稿态，两者并行互不影响
/quit debug
- 适用范围：在当前处于 debug 模式的 session 中使用
- 行为：退出 debug 模式，智能体切换回线上发布版本
- 会话影响：退出后不清空 session 上下文，对话记录保留
